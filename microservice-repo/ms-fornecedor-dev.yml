spring:  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/db_fornecedor?useTimezone=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    username: root  